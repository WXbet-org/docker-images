# Ubuntu Pro token -- NEVER commit!
pro-attach-config.yaml

# Baked-in OE sources seed (~11 GB) -- kept out of git, populated per-server
# via rsync from the legacy builder. See dreamos-buildsystem-ubnt18/README.md.
sources-seed/

# Local overrides
*.local
.env

# Populated at build time by oea-buildsystem-repo/build-repo.sh -- the
# checked-out oe-alliance/build-enviroment repository with initialised
# submodules. Regenerated per branch build, never committed.
oea-buildsystem-repo/repo/
